# Official content

Each official scene belongs in its own stable-ID directory and must include `provenance.json` conforming to `docs/CONTENT_POLICY.md`. Do not add placeholder copyrighted clips.

Keep source media immutable. Project JSON, session state, and player recordings belong in their own runtime storage locations rather than inside official content packages.

